#include <stdio.h>
#include <math.h>

int main() {
 
    int xf, yf, xe, ye, r1, r2, ve;
    double distance;
    
    while (scanf("%d%d%d%d%d%d%d", &xf, &yf, &xe, &ye, &ve, &r1, &r2) != EOF) {
        distance = sqrt(pow(xe - xf, 2) + pow((ye - yf), 2)) + (ve * 1.5);
        
        if(r1 + r2 > distance)
            printf("Y\n");
        else 
            printf("N\n");
    }
    
    return 0;
}
