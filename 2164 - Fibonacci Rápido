#include <stdio.h>
#include <math.h>

int main() {
 
    int n;
    double fibo;
    
    scanf("%d", &n);
    
    fibo = (pow(((1+sqrt(5))/2.0), n) - pow(((1-sqrt(5))/2.0), n))/sqrt(5);
    
    
    printf("%.1f\n", fibo);
    return 0;
}
